Metadata providers:
- LibriVox free audiobook API (default first pass)
- OpenLibrary public search API (default first pass)
- Google Books API as secondary fallback

Audible scraping is intentionally not enabled in this MVP because scraping reliability and terms can vary.
